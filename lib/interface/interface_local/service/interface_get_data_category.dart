abstract class interfaceGetDataCategory{
  GetDataCategory({
    bool testing = false,
    required bool loadingApiService,
  });

  GetDataCategoryNoParameters({
    bool testing = false,
    required bool loadingApiService,
  });
}